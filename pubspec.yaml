name: cupertino_icons_module
description: "This module is a module of the juneflow framework about the cupertino_icons package."
#publish_to: 'none'
repository: https://github.com/juneflow-songdo/cupertino_icons_module

version: 1.0.1

environment:
  sdk: '>=3.3.1 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_web_plugins:
    sdk: flutter
  go_router: ^13.2.0
  cupertino_icons: ^1.0.6 #@add
  june_flow_util:


dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true